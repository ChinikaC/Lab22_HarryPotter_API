const Character = ({character}) => {

    // const handleClick = (e) => {
    //     updateCharacterList(character.name)
    // }

    return(
        <li>
            {character.name}
    
        </li>
    );

}

export default Character;